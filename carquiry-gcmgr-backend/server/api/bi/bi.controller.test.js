import {expect} from 'chai';

import TestHelper from '../../tests/helpers';
const test = new TestHelper();

describe('bi.controller.js', function () {
  // Init DB for card controller
  test.initDb();
});
